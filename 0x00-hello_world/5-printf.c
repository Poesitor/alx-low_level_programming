#include <stdio.h>

/**
 * main - function that prints with printf
 *
 * Return: 0 (Success)
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
